from deepchainapps.data import Pathogen
from deepchainapps.models import MLP

dataset = Pathogen.load_data()
data_embeded = 
X_train, X_val, y_train, y_val = train_test_split(X, y_class, test_size=0.3)
